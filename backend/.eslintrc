{
  "extends": "airbnb-base",
  "rules": {
    "no-underscore-dangle": ["error", { "allow": ["_id"] }],
    "no-console": ["error", { "allow": ["log"] }],
    "linebreak-style": ["error", "windows"], // o "unix"
    "no-unused-vars": ["error", { "argsIgnorePattern": "next" }]
  }
}
